[package]
name = "quarrel"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui = { git = "https://github.com/zed-industries/zed" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
